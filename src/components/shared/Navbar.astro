---
import { Astronav } from "astro-navbar";
import Logo from "../ui/logo.astro";
const links = ["Home", "Resources", "Pricing"];
---

<astro:head>
    <style>
        .bg-scroll {
            background-color: white;
            /* Add any other styling you need for the scrolled state */
        }
    </style>
</astro:head>

<script>
    function handleScroll() {
        const navbar = document.querySelector("#primary-navbar");
        if (window.scrollY > 0) {
            navbar?.classList.add("bg-white");
        } else {
            navbar?.classList.remove("bg-white");
        }
    }
    window.onscroll = () => {
        handleScroll();
    };

    window.removeEventListener("scroll", handleScroll);
</script>

{/*bg-white */}

<nav
    id="primary-navbar"
    class="h-20 w-full fixed top-0 flex justify-center z-50 transition-all"
>
<div class="container-main w-full  flex  justify-start  items-center">

    <Astronav>
        <a href="/" class="mr-12"><Logo /></a>
        <div class="flex gap-8">
            {
                links.map((link) => (
                    <a href={`/${link.toLowerCase()}`} class="font-semibold hover:text-gray-800 transition-all text-base text-gray-600 cursor-pointer">
                        {link}
                    </a>
                ))
            }
        </div>
        <div class="ml-auto flex gap-4">
            <button class="button-secondary"> Log in</button>
            <button class="button-primary"> Sign up</button>
        </div>
    </Astronav>
</div>
</nav>
